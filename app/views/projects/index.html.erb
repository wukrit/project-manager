<h1>Welcome, <%= current_user.present? ? current_user.email : 'Stranger' %> </h1>

<% @projects.each do |project| %>
  <ul>
    <li>
      <%= project.title %> - <%= project.description %>
    </li>
  </ul>
<% end %>
